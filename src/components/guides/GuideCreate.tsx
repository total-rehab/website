import { CreateForm } from '@jambff/ra-components';
import { FC } from 'react';
import { GuideInputs } from './GuideInputs';

export const GuideCreate: FC = () => (
  <CreateForm>
    <GuideInputs />
  </CreateForm>
);
