import { FC } from 'react';
import { CreateForm } from '@jambff/ra-components';
import { MediaInputs } from './MediaInputs';

export const MediaCreate: FC = () => (
  <CreateForm>
    <MediaInputs />
  </CreateForm>
);
