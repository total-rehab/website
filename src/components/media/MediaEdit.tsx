import { FC } from 'react';
import { EditForm } from '@jambff/ra-components';
import { MediaInputs } from './MediaInputs';

export const MediaEdit: FC = () => (
  <EditForm>
    <MediaInputs />
  </EditForm>
);
